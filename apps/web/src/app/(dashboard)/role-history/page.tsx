import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Role History — OpenATS',
  description:
    'Browse historical accepted candidates and find similar profiles using semantic search.',
};

export { default } from './RoleHistoryPage';
