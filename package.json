{
  "name": "openats",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm --parallel -r run build"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "pnpm": {
    "only-built-dependencies": [
      "canvas",
      "esbuild",
      "msgpackr-extract"
    ]
  }
}
